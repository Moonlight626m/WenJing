# 前端端点配置（Next.js 构建期注入，NEXT_PUBLIC_ 前缀）
#
# 同源部署（经反向代理统一域名）留空即可：浏览器以相对 /api、/ws 访问。
# 若不设置任何变量，本地开发默认指向 http://localhost:8000。
#
# 前后端不同源时取消注释并填写后端地址：
# NEXT_PUBLIC_API_BASE=http://localhost:8000
# NEXT_PUBLIC_WS_BASE=ws://localhost:8000
#
# 前端日志级别（debug < info < warning < error），默认 info：
# NEXT_PUBLIC_LOG_LEVEL=info
